<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bb.quanlycc">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/apartment"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeRegist">
        <activity android:name=".ThanhToanChiTietActivity"></activity>
        <activity android:name=".DichVuChiTietActivity" />
        <activity android:name=".DichVuActivity" />
        <activity android:name=".HomeAdminActivity" />
        <activity android:name=".NewsDetailActivity" />
        <activity android:name=".ThongTinActivity" />
        <activity android:name=".LienHeActivity" />
        <activity android:name=".TienNuocActivity" />
        <activity android:name=".TienDienActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppThemeRegist" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppThemeLogin" />
    </application>

</manifest>